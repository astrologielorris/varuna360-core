"""Human Design BodyGraph view.

``hd_geometry`` is the layout (canvas coordinates, no Qt), ``hd_bodygraph_view`` is the
QGraphicsView that paints it from an HDModel. Nothing here computes a chart.
"""
